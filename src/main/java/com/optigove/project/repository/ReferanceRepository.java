package com.optigove.project.repository;

import com.optigove.project.model.Referance;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReferanceRepository extends JpaRepository<Referance, Long> {
}
